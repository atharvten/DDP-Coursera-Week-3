---
title: "DDP Week 3 plotly"
author: "Atharv Tendolkar"
date: "23/11/2021"
output: html_document
---

## Code for the project

Or code involves the closing prices of major European stock indices in the years of 1991 to 1998.

```{r, echo=TRUE, eval=FALSE}
library(plotly)
library(tidyr)
library(dplyr)
data("EuStockMarkets")
stocks <- as.data.frame(EuStockMarkets) %>%
  gather(index, price) %>%
  mutate(time = rep(time(EuStockMarkets), 4))
plot_ly(stocks, x = ~time, y = ~price, color = ~index, mode = "lines")
```

## Plot to display

```{r, echo=FALSE, eval=TRUE, message=FALSE}
library(plotly)
library(tidyr)
library(dplyr)
data("EuStockMarkets")
stocks <- as.data.frame(EuStockMarkets) %>%
  gather(index, price) %>%
  mutate(time = rep(time(EuStockMarkets), 4))
plot_ly(stocks, x = ~time, y = ~price, color = ~index, mode = "lines")
```